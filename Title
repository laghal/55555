class Title
{
  boolean press = false;
  boolean gameStarted = false;
  Room titleRoom = new Room();
  float background = 245; 
  
  void draw()
  {    
    background(background);
    titleRoom.draw();
    textSize(height/5);
    fill(5,5,5,background);
    text("55555", width/2, (height/2)-(height/20));
    textSize(height/25);
    fill(55,55,55,background);
    text("Press any key to begin", width/2, (height/2)+(height/10));
    if(press && background>0)
    {
      background-=2;
    }
    else if(background<=0)
    {
      gameStarted = true;
      getlost(1);
      ost.setMazeMusic();
    }
  }
  
  void keyPressed()
  {
    if(keyPressed)
    {
      press = true;
    }
  }
}
