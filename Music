class Music
{
  AudioPlayer title;
  AudioPlayer start;
  AudioPlayer maze;
  boolean titleSet = false;
  boolean startSet = false;
  boolean mazeSet = false;
  
  Music()
  {
    title = minim.loadFile("ost/Title.mp3", 2048);
    start = minim.loadFile("ost/Start.mp3", 2048);
    maze = minim.loadFile("ost/Maze.mp3", 2048);
  }
  
  void setTitleMusic()
  {
    if(!titleSet)
    {
      title.loop();
      titleSet = true;
    }
  }
  void startSound()
  {
    title.close();
    start.play();
    startSet = true;
  }
  void setMazeMusic()
  {
    start.close();
    if(!mazeSet)
    {
      maze.loop();
      mazeSet = true;
    }
  }
}
